"""bridgev2.llm.prompt — Montagem de prompt/persona + schema de saída (Fase 5)."""
from __future__ import annotations
# TODO Fase 5: build_prompt(text, context, config) -> list[dict]
# Schema de saída: {"reply": str, "expression_id": int|null, "action": int|null, ...}
